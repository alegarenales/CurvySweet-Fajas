<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Pago cancelado | CurvySweet</title>
  </head>
  <body>
    <main class="status-page">
      <section class="status-card">
        <p class="status-kicker">CurvySweet</p>
        <h1>Pago cancelado</h1>
        <p>No se realizo ningun cobro. Puedes intentarlo otra vez cuando quieras.</p>
        <a href="/">Volver a la tienda</a>
      </section>
    </main>
  </body>
</html>

<style>
  :global(html, body) {
    margin: 0;
    min-height: 100%;
    font-family: "Segoe UI", sans-serif;
    background:
      radial-gradient(circle at top left, rgba(255, 199, 221, 0.85), transparent 28%),
      radial-gradient(circle at 85% 18%, rgba(255, 223, 205, 0.9), transparent 26%),
      linear-gradient(135deg, #fff7fc, #fff0eb);
    color: #2b1630;
  }

  .status-page {
    min-height: 100vh;
    padding: 20px;
    display: grid;
    place-items: center;
    box-sizing: border-box;
  }

  .status-card {
    width: min(100%, 520px);
    padding: 28px;
    border-radius: 28px;
    background: rgba(255, 255, 255, 0.82);
    border: 1px solid rgba(255, 255, 255, 0.7);
    box-shadow: 0 24px 70px rgba(154, 73, 116, 0.16);
    backdrop-filter: blur(20px);
  }

  .status-kicker {
    margin: 0 0 10px;
    text-transform: uppercase;
    letter-spacing: 0.16em;
    font-size: 0.76rem;
    font-weight: 800;
    color: #c93178;
  }

  h1 {
    margin: 0 0 14px;
    font-size: clamp(2rem, 8vw, 3.2rem);
    line-height: 0.98;
    letter-spacing: -0.04em;
  }

  p {
    margin: 0;
    line-height: 1.7;
    color: #6d5568;
  }

  a {
    display: inline-flex;
    margin-top: 22px;
    padding: 14px 18px;
    border-radius: 16px;
    background: linear-gradient(135deg, #e45596, #c93178);
    color: white;
    text-decoration: none;
    font-weight: 800;
  }

  @media (max-width: 480px) {
    .status-page {
      padding: 14px;
    }

    .status-card {
      padding: 22px 18px;
      border-radius: 22px;
    }

    a {
      width: 100%;
      justify-content: center;
      box-sizing: border-box;
    }
  }
</style>
